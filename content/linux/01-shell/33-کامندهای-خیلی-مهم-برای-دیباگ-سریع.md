---
title: "33. کامندهای خیلی مهم برای دیباگ سریع"
description: "33. کامندهای خیلی مهم برای دیباگ سریع"
---

# 33. کامندهای خیلی مهم برای دیباگ سریع

### چه چیزی روی این پورت است؟

```bash
sudo lsof -i :3000
```

### چرا build fail شده؟

```bash
npm run build 2>&1 | tee build.log
```

بعد:

```bash
grep -i "error" build.log
```

### آیا API بالا است؟

```bash
curl -I http://localhost:3000
```

### آیا DNS درست است؟

```bash
dig example.com
```

### آیا SSL کار می‌کند؟

```bash
curl -Iv https://example.com
```

### آخرین لاگ‌های سرویس چیست؟

```bash
sudo journalctl -u my-api -n 100 --no-pager
```

### فضای سرور پر شده؟

```bash
df -h
```

### چه پوشه‌ای فضا را خورده؟

```bash
du -h --max-depth=1 /var | sort -h
```

---
